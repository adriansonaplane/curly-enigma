{
  "private": true,
  "scripts": {
    "compile:models": "node tools/compile-models.js && node tools/validate-models.js",
    "test:browser": "playwright test -c tests/browser/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
